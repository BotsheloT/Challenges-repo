//Cirlce Area function
const calcArea = radius => 3.14 * radius**2;

//Square Area function
const squareArea = side => side**side;

//Traingle Area function
const triArea = (base, height) => {
    return 0.5*base*height;
}

//Variables for buttons
